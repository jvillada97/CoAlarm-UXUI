<!-- res/drawable/image_button_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado presionado (cuando el botón está en "hover" o presionado) -->
    <item android:state_pressed="true" android:drawable="@drawable/ic_add_black" />

    <!-- Estado normal (cuando el botón no está interactuando) -->
    <item android:drawable="@drawable/ic_add" />
</selector>
